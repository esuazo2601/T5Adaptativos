#include <iostream>
#include <vector>
#include <string>

using namespace std;

int distancia(char a, char b);
char menor_distancia(vector<pair<int, char>>& dist, int j);
pair<string,int> Greedy_probabilista(vector<string>entrada, int deter_int);
int generate_random_value();
